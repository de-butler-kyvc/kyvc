spring:
  config:
    activate:
      on-profile: dev
  flyway:
    enabled: ${FLYWAY_ENABLED:false}
  jpa:
    show-sql: false

logging:
  level:
    com.kyvc.backendadmin: DEBUG

kyvc:
  backend:
    base-url: ${KYVC_BACKEND_BASE_URL:http://kyvc-backend-dev:8082}
